import styled from 'styled-components';
import { device } from '../Responsive';

export const ProjectsContainerWrapper = styled.section`
  position: relative;
  min-height: 90vh;
  padding: 5rem 5%;
`;
