import styled from 'styled-components';
import { Colors } from '@/src/styled/components/base/Colors';

export const MainButton = styled.div`
  padding: 1rem 1.8rem;
  background: ${Colors.PrimaryColor};
  color: ${Colors.PrimaryTextColor};
  font-size: 1.6rem;
`;
