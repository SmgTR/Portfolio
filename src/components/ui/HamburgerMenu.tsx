import { FC } from 'react';

const HamburgerMenu: FC = ({}) => {
  return (
    <div className="hamburger mouseHover circle">
      <div className="hamburger__patty"></div>
    </div>
  );
};

export default HamburgerMenu;
